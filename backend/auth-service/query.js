const { Client } = require('pg'); const client = new Client({ host: 'localhost', port: 5432, user: 'postgres', password: '200600', database: 'esap_db' }); client.connect().then(() => client.query('SELECT table_schema, table_name FROM information_schema.tables WHERE table_schema IN (''auth'', ''legal'', ''public'')')).then(res => { console.log(res.rows); client.end(); }).catch(console.error);
